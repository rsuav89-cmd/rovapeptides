# RovaPeptides — Master Task List

## SEO & Layout Cleanup

- [x] Favicon & Open Graph (OG) Meta Assets — dynamic favicon (app/icon.tsx) + Apple touch icon (app/apple-icon.tsx) via the Next.js metadata file convention; Open Graph + Twitter Card metadata added in app/layout.tsx, with the share image generated by app/opengraph-image.tsx so links shared on iMessage, WhatsApp, Twitter, etc. show a high-res brand card instead of a blank box.
- [x] Image Alt Text — components/ProductImage.tsx (used by Catalog.tsx, ProductCard.tsx, ProductModal.tsx, and HeroShowcase.tsx) now generates descriptive alt text from product name, subtitle, mass, purity, and batch.
- [x] Footer Cleanup — removed the "Encrypted · High-Risk Merchant Compliant" text block from components/Footer.tsx. The required "For Research Use Only — Not for Human Consumption" compliance disclosures in the footer compliance band, the footer bottom bar, and the NoticeBar.tsx marquee were intentionally left untouched.
- [x] Commit & Push — all changes committed directly to main.
